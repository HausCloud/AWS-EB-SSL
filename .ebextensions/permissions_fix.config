# # Optional fix for any possible permissions errors
# container_commands:
#   01_chmod:
#     command: find / -type f -iname '00_ssl_setup_nginx.sh' -exec chmod +x {} \;

# container_commands:
#   01_chmod:
#     command: find / -type f -iname '01_ssl_setup_apache.sh' -exec chmod +x {} \;